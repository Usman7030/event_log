"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const shared_pb_1 = require("../../generated/shared_pb");
const streams_grpc_pb_1 = require("../../generated/streams_grpc_pb");
const streams_pb_1 = require("../../generated/streams_pb");
const utils_1 = require("../utils");
const constants_1 = require("../constants");
const Client_1 = require("../Client");
const ReadStream_1 = require("./utils/ReadStream");
Client_1.Client.prototype.readAll = function ({ maxCount = Number.MAX_SAFE_INTEGER, fromPosition = constants_1.START, resolveLinkTos = false, direction = constants_1.FORWARDS, ...baseOptions } = {}, readableOptions = {}) {
    const req = new streams_pb_1.ReadReq();
    const options = new streams_pb_1.ReadReq.Options();
    const uuidOption = new streams_pb_1.ReadReq.Options.UUIDOption();
    uuidOption.setString(new shared_pb_1.Empty());
    const allOptions = new streams_pb_1.ReadReq.Options.AllOptions();
    switch (fromPosition) {
        case "start": {
            allOptions.setStart(new shared_pb_1.Empty());
            break;
        }
        case "end": {
            allOptions.setEnd(new shared_pb_1.Empty());
            break;
        }
        default: {
            const pos = new streams_pb_1.ReadReq.Options.Position();
            pos.setCommitPosition(fromPosition.commit.toString(10));
            pos.setPreparePosition(fromPosition.prepare.toString(10));
            allOptions.setPosition(pos);
            break;
        }
    }
    options.setAll(allOptions);
    options.setResolveLinks(resolveLinkTos);
    options.setCount(maxCount.toString(10));
    options.setUuidOption(uuidOption);
    options.setNoFilter(new shared_pb_1.Empty());
    switch (direction) {
        case constants_1.FORWARDS: {
            options.setReadDirection(0);
            break;
        }
        case constants_1.BACKWARDS: {
            options.setReadDirection(1);
            break;
        }
    }
    req.setOptions(options);
    utils_1.debug.command("readAll: %O", {
        maxCount,
        options: {
            fromPosition,
            direction,
            ...baseOptions,
        },
    });
    utils_1.debug.command_grpc("readAll: %g", req);
    const createGRPCStream = this.GRPCStreamCreator(streams_grpc_pb_1.StreamsClient, "readAll", (client) => client.read(req, ...this.callArguments(baseOptions, {
        deadline: Infinity,
    })));
    return new ReadStream_1.ReadStream(createGRPCStream, utils_1.convertGrpcEvent, readableOptions);
};
